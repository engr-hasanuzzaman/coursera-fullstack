// type any means, type checker stop type checking. So you can push anythinkg in any
// We should avoid using "any"
const anys = [];
anys.push(1);
anys.push("oh no");
anys.push({ anything: "goes" });
alert(anys);

// 